{
  "name": "@ai-dynamo/snapshot-benchmark-dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.22.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "test": "vitest run",
    "test:browser": "playwright test",
    "typecheck": "tsc --noEmit",
    "validate-data": "node scripts/validate-history.ts"
  },
  "dependencies": {
    "chart.js": "4.5.1"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "25.5.0",
    "typescript": "6.0.2",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
